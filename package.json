{
  "dependencies": {
    "react-datepicker": "^4.21.0"
  },
  "scripts": {
    "install": "npm install --save-dev concurrently && npm install --save-dev @babel/plugin-proposal-private-property-in-object && npm run install-frontend && npm run install-backend",
    "start": "concurrently \"npm run start-frontend\" \"npm run start-backend\"",
    "start-frontend": "cd frontend && npm start",
    "start-backend": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install && npm install express && npm install nodemailer && npm install dotenv",
    "install-frontend": "cd frontend && npm install && npm install react-icons --save"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "concurrently": "^8.2.2"
  }
}
